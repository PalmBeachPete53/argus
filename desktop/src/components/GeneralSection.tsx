// Minimal General section — establishes the extensible Settings structure.
// No invented parameters: nothing to configure yet.
export default function GeneralSection() {
  return (
    <div>
      <div className="data-browser-muted">Nothing to configure yet.</div>
    </div>
  );
}
