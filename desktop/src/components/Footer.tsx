// V1 footer is intentionally empty — reserved for status / pipeline activity /
// errors / storage info in future GUI phases.
export default function Footer() {
  return <footer className="footer" aria-hidden="true" />;
}
